apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dotnet-quote
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/admbmg/sample-apps.git
    targetRevision: HEAD
    path: dotnet-quote/manifests
  destination:
    server: https://kubernetes.default.svc
    namespace: demo-quote
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
